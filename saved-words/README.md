# Saved words

Words starred in the game (☆) collect on the **Saved** tab. Use **Download .txt**
there and drop the file in this folder.

Files use the same `word | part of speech | definition` format as
`scripts/words/`, so a list can be copied into a word file directly.
